<%= turbo_frame_tag dom_id(feed, folder.id) do %>
  <%= render(ButtonComponent.new(text: folder.subscribe?(feed) ? "削除" : "追加", variant: folder.subscribe?(feed) ? "primary" : "outline", href: feed_feed_subscriptions_path(feed, folder_id: folder.id), data: { turbo_method: :post })) %>
<% end %>
