<div class="flex items-center h-screen">
  <div class="grid md:grid-cols-2 items-center w-full">
    <div class="flex flex-col items-center px-6">
      <div class="flex flex-col items-center gap-6 mb-4">
        <%= render(HeadingComponent.new(size: "h1", text: t(".sign_up"))) %>
        <div class="text-center">
          <p><%= t(".description") %><p>
          <p><%= sanitize(t(".login", login_path: link_to(t(".here"), new_session_path(resource_name), class: "text-blue-400"))) %><p>
        </div>
      </div>

      <div class="w-full max-w-lg">
        <div class="bg-primary-light rounded p-8">
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="mb-4">
              <%= render(FormFieldComponent.new(form: f, attribute_key: :email, placeholder: "メールアドレス")) %>
            </div>
            <div class="mb-8">
              <%= render(FormFieldComponent.new(form: f, field_type: :password_field, attribute_key: :password, placeholder: "パスワード")) %>
            </div>

            <div class="actions">
              <%= f.submit t(".submit"), class: "py-2 px-4 bg-primary w-full text-white font-bold rounded disabled:bg-gray-400" %>
            </div>
          <% end %>
          <div class="relative py-4">
            <div class="absolute inset-0 flex items-center">
              <span class="w-full border-t"></span>
            </div>
            <div class="relative flex justify-center text-xs">
              <span class="text-slate-500 px-2 bg-[#f1fcf1]">
                OR
              </span>
            </div>
          </div>

          <button
            aria-label="Sign in with Google"
            class="flex justify-center items-center bg-white border border-button-border-light rounded p-0.5 pr-3 w-full">
            <div class="flex items-center justify-center bg-white w-9 h-9 rounded-l">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="w-5 h-5">
                <title>Sign in with Google</title>
                <desc>Google G Logo</desc>
                <path
                  d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"
                  class="fill-google-logo-blue"></path>
                <path
                  d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
                  class="fill-google-logo-green"></path>
                <path
                  d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"
                  class="fill-google-logo-yellow"></path>
                <path
                  d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"
                  class="fill-google-logo-red"></path>
              </svg>
            </div>
            <span class="text-sm text-google-text-gray tracking-wider">Sign in with Google</span>
          </button>
        </div>
      </div>
    </div>
    <div class="hidden md:block flex flex-col items-center px-6">
      <%= image_tag "sign_up" %>
      <ul class="">
        <li>いい感じの紹介テキストを入れないなあああああ</li>
        <li>いい感じの紹介テキストを入れないなあああああ</li>
        <li>いい感じの紹介テキストを入れないなあああああ</li>
      </ul>
    </div>
  </div>
</div>
